<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dynamic Page</title>
    <link rel="stylesheet" href="/css/Dynamic.css">
</head>

<body>
    {{> navbar}}
    <main>
        <div class="main">
            <div class="container">
                <div class="image">
                    <!-- Display the book's image -->
                    <img src="/uploads/{{book.imagePath}}" alt="{{book.bookname}}">
                </div>
                <div class="info">
                    <!-- Display the book's details -->
                    <h4>{{book.standard}} {{book.bookname}}</h4>
                    <h5>Location: <span>{{book.location}}</span></h5>
                    <p>Pincode: <span>{{book.pincode}}</span></p>
                    <!-- Add a link to call the user's phone number -->
                    <a href="tel:{{book.number}}">CLICK TO CALL</a>
                </div>
            </div>
        </div>
    </main>
    {{> footer}}
</body>

</html>